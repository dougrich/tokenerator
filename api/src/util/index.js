const namedFunc = require('./namedFunc')
const wrapFunc = require('./wrapFunc')
const stalecache = require('./stalecache')
const compileMiddleware = require('./compileMiddleware')

module.exports = {
  namedFunc,
  wrapFunc,
  compileMiddleware,
  stalecache
}